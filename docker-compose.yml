services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - TBA_API_KEY=${TBA_API_KEY:-}
      - NEXUS_API_KEY=${NEXUS_API_KEY:-}
    restart: unless-stopped
